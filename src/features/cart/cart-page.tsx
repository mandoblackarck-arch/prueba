'use client';
import Link from 'next/link';
import { useAppDispatch, useAppSelector } from '@/store/hooks';
import { removeFromCart } from '@/store/slices/cart-slice';
const money = new Intl.NumberFormat('es-MX', { style: 'currency', currency: 'MXN', maximumFractionDigits: 0 });
export function CartPage() { const items = useAppSelector(s => s.cart.items); const total = useAppSelector(s => s.cart.items.reduce((sum, item) => sum + item.price * item.quantity, 0)); const dispatch = useAppDispatch(); return <section className="section page"><p className="eyebrow">TU BOLSA</p><h1>{items.length ? 'Unos detalles más' : 'Tu bolsa está vacía'}</h1>{items.length ? <div className="cart-layout"><div className="cart-items">{items.map(x => <article className="cart-item" key={x.id}><img src={x.image} alt="" /><div><h3>{x.name}</h3><p>{x.quantity} unidad{x.quantity > 1 ? 'es' : ''} · {money.format(x.price)}</p><button className="text-button" onClick={() => dispatch(removeFromCart(x.id))}>Eliminar</button></div><b>{money.format(x.price * x.quantity)}</b></article>)}</div><aside className="summary"><h2>Resumen</h2><div><span>Subtotal</span><span>{money.format(total)}</span></div><div><span>Envío</span><span>Gratis</span></div><hr /><div className="total"><b>Total</b><b>{money.format(total)}</b></div><Link className="button full" href="/checkout">Continuar al pago</Link></aside></div> : <Link className="button" href="/productos">Explorar productos</Link>}</section>; }
