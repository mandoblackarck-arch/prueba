'use client';
import { useSearchParams } from 'next/navigation'; import { FormEvent, useState } from 'react';
export function OrderLookup() { const params = useSearchParams(); const [number, setNumber] = useState(params.get('numero') ?? ''); const [shown, setShown] = useState(Boolean(params.get('numero'))); const submit = (e: FormEvent) => { e.preventDefault(); setShown(true); }; return <section className="form-page"><div className="form-card"><p className="eyebrow">SEGUIMIENTO</p><h1>Consulta tu pedido</h1><p>Escribe el número que recibiste al confirmar la compra.</p><form onSubmit={submit}><label>Número de pedido<input value={number} onChange={e => setNumber(e.target.value.toUpperCase())} required placeholder="LM-10483" /></label><button className="button full">Consultar estado</button></form>{shown && <div className="order-status"><p><span className="status-dot" /> Pedido confirmado</p><h2>{number}</h2><span>Estamos preparando tus productos. Te avisaremos cuando salgan a entrega.</span></div>}</div></section>; }
